import React from 'react';
import ReactDOM from 'react-dom/client';

//used components
import App from './App';

const APP_MOUNT = ReactDOM.createRoot(document.getElementById('app-mount'));
APP_MOUNT.render(
  <App />
);
